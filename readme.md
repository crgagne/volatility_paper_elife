
##

Bifactor modeling.
The individual item level data for the bifactor model can be found for.

The code to prepropoccess the data can be found in XX. (maybe don't include; and just have processed data.)
The code to fit the bifactor model can be found in jupyer R notebook. (see below for installation)

Figures XYZ in the paper can be found in jupyter notebooks.

Behavioral modeling.
Individual data can be found XYZ.
Fitting the main behavioral model can be done by running the python code (see below for)


Behavioral model can be fit or downloaded (from OpenScience..) and moved to ... 





## Installations

Behavioral modeling:  
`conda create --name pymc3_3.6`
`conda activate pymc3_3.6`
`conda install pymc3=3.6`
`conda install jupyter`
`conda install seaborn`
`conda install statsmodels`

Bifactor modeling:
`conda create --name env_r --file requirements_r.txt`
`conda activate env_r`


## Running Jupyter notebooks with Python Kernel (Bifactor analyses)

Most jupyter notebooks should be run using the pymc3_3.6 environment. The only exception is the Bifactor modeling notebook.
